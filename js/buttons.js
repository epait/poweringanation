var pop = Popcorn( "#mainTrack" );
	pop.code({
		start: 15,
		onStart: function() {
			$('#dragButton').toggle();
		},
	});

	pop.code({
		start: 25,
		onStart: function() {
			$('#dragButton').toggle();
		},
	});

	pop.code({
		start: 40,
		onStart: function() {
			$('#dragButton').toggle();
		},
	});
